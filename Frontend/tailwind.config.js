/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./src/**/*.{html,ts}" // Tells Tailwind to scan these files for class names
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}